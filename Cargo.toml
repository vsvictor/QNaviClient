[package]
name = "QNaviClient"
version = "0.1.0"
edition = "2021"

[dependencies]
# h3 from git (same approach as у вашому QNavi репо)
h3 = { git = "https://github.com/hyperium/h3.git", package = "h3" }
h3-quinn = { package = "h3-quinn", git = "https://github.com/hyperium/h3.git" }

# Quinn family (match h3-quinn expectations)
quinn = "0.11"
quinn-proto = "0.11"

# Core crates (match your working repo)
http = "1.3"
bytes = "1"

# Tokio, tracing, etc.
tokio = { version = "1", features = ["rt-multi-thread", "macros", "io-util", "signal"], default-features = false }

tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "std"] }

anyhow = "1"
rcgen = "0.9"

# Serde + uuid + chrono
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

parking_lot = "0.12"
rustls-pki-types = "1.13.0"
futures-util = "0.3.31"

[profile.dev]
overflow-checks = true
opt-level = 0